<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_overlay_gradient">

    <!-- Background Image -->
    <ImageView
        android:id="@+id/ivBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="gone"
        android:alpha="0.3" />

    <!-- Dark scrim -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_overlay_scrim" />

    <!-- Emergency button (top-end, lock screen only) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnEmergency"
        style="@style/Widget.Material3.Button.ElevatedButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="56dp"
        android:layout_marginEnd="20dp"
        android:text="✕ Emergency"
        android:textSize="12sp"
        android:backgroundTint="@color/color_emergency"
        android:textColor="@color/white"
        app:icon="@drawable/ic_close"
        app:iconTint="@color/white"
        app:iconSize="14dp"
        android:visibility="gone" />

    <!-- Trigger badge (top-start) -->
    <TextView
        android:id="@+id/tvTriggerBadge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_marginTop="64dp"
        android:layout_marginStart="20dp"
        android:background="@drawable/bg_badge"
        android:paddingHorizontal="12dp"
        android:paddingVertical="6dp"
        android:text="📱 Phone Unlocked"
        android:textSize="12sp"
        android:textColor="@color/white"
        android:fontFamily="sans-serif-medium" />

    <!-- Main content card -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginHorizontal="24dp"
        app:cardCornerRadius="24dp"
        app:cardBackgroundColor="@color/overlay_card_bg"
        app:cardElevation="20dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="32dp">

            <!-- Mindfulness icon -->
            <ImageView
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_mindful"
                app:tint="?attr/colorPrimary"
                android:layout_marginBottom="20dp" />

            <!-- Title -->
            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Pause &amp; Reflect"
                android:textSize="28sp"
                android:textColor="?attr/colorOnSurface"
                android:textStyle="bold"
                android:gravity="center"
                android:fontFamily="sans-serif-medium"
                android:layout_marginBottom="16dp" />

            <!-- Accent divider -->
            <View
                android:layout_width="48dp"
                android:layout_height="3dp"
                android:layout_gravity="center"
                android:background="?attr/colorPrimary"
                android:layout_marginBottom="20dp" />

            <!-- Quote -->
            <TextView
                android:id="@+id/tvQuote"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Take a breath. Do you really need to check your phone right now?"
                android:textSize="16sp"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:gravity="center"
                android:lineSpacingMultiplier="1.4"
                android:layout_marginBottom="28dp" />

            <!-- Timer -->
            <FrameLayout
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:layout_gravity="center"
                android:layout_marginBottom="4dp">

                <View
                    android:layout_width="72dp"
                    android:layout_height="72dp"
                    android:background="@drawable/bg_timer_circle" />

                <TextView
                    android:id="@+id/tvTimer"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:text="10"
                    android:textSize="26sp"
                    android:textStyle="bold"
                    android:textColor="?attr/colorPrimary"
                    android:gravity="center"
                    android:fontFamily="sans-serif-medium" />

            </FrameLayout>

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="seconds"
                android:textSize="11sp"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:gravity="center"
                android:layout_marginBottom="24dp" />

            <!-- Redirect to healthy app button (shown only if redirect app is set) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnRedirect"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Open Alternative App"
                android:textSize="14sp"
                android:visibility="gone"
                app:icon="@drawable/ic_redirect"
                app:iconGravity="textStart"
                app:iconSize="18dp"
                style="@style/Widget.Material3.Button.TonalButton" />

        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Progress bar at bottom -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingBottom="32dp"
        android:paddingHorizontal="24dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Screen closes automatically"
            android:textSize="11sp"
            android:textColor="#80FFFFFF"
            android:gravity="center"
            android:layout_marginBottom="8dp" />

        <ProgressBar
            android:id="@+id/progressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="6dp"
            android:progressDrawable="@drawable/progress_bar_drawable"
            android:max="1000"
            android:progress="1000" />

    </LinearLayout>

</FrameLayout>
